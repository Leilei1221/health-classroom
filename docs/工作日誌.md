# 工作日誌

最新的在最上面。格式見 `docs/協作規則.md` 第四節。
每次開工前先讀這裡的最後三筆，以及 `docs/協作規則.md`。

---

## 2026-09-25｜Claude Code｜剩餘工作分配表補上進度欄

**分支**：`claude/funny-fermat-qfo6ik`
**狀態**：完成，待蕾蕾老師確認合併

**做了什麼**：

- 把 `docs/協作規則.md` 剩餘工作分配表的每一項，對照 main（`830b156`）的程式碼與 `docs/交班文件_給Codex.md` 第七節規格查證，新增「進度」欄
- 結果：0、1、2、4、5 完成；4b、5b、6 部分完成；3、7a 未做；7b 刻意暫緩

**動到的檔案**：

- `docs/協作規則.md`（修改分配表）
- `docs/工作日誌.md`（本筆）

**沒動到的檔案**（確認用）：

- 所有程式碼、migration 未修改；鎖定清單內的檔案未修改

**資料庫**：無變更

**測試**：只改文件，未跑回歸

**發現的問題或疑問**：

1. `src/health/analysis/engine.ts` 有幾段學生看得到的文案不在建議庫裡：`FALLBACK_STATE`（`skip_breakfast`、`sedentary`、`diet_type_ABCDF`、`whr_amber` 的現況文案）、`FIRST_STEP`（血壓、BMI、腰臀比的第一步），以及通用備用句。建議庫文件寫明「不要自行新增條目，健康建議由授課教師負責」，要不要換成教師提供的文案，待蕾蕾老師決定。這次沒有改
2. 打卡頁沒有擋未來日期（規格要求「可補登過去、不能補未來」），畫面與資料庫都沒有相應的檢查
3. 5b「一次點擊完成」目前是表單形式，是否符合規格【請確認】

**下一步**：

- 蕾蕾老師：確認合併；決定第 1 點的文案怎麼處理
- Codex：3、4b 落差檢查、5b 擋未來日期（畫面）、6 熱區、7a

---

## 2026-09-25｜Claude Code｜文件事實校正（給 AI 讀的說明文件）

**分支**：`claude/funny-fermat-qfo6ik`
**狀態**：完成，待蕾蕾老師確認合併

**做了什麼**：

- 把給 AI 讀的文件逐份跟程式碼、遠端分支比對，修掉五處已經過時的事實
- 存檔點改指向實際存在的分支 `handoff/claude-2026-09-21`（tag 在遠端不存在）
- 第一版規格與待辦的 BMI 描述改成現行做法（國健署男性 15～18 歲四列，數字以 `rules.ts` 為準）
- 自我檢測交接的部署段落不再寫「push 到 main」，改成與協作規則一致
- 剩餘工作分配表下註明「不是即時進度」，並指出第 4、5 項的表已建好

**動到的檔案**：

- `docs/協作規則.md`、`docs/健護課健康管理系統_第一版規格.md`、`docs/待辦.md`、`docs/自我檢測與餐盤_實作交接.md`（修改）
- `docs/工作日誌.md`（本筆）

**沒動到的檔案**（確認用）：

- 所有程式碼、migration 未修改；鎖定清單內的檔案未修改

**資料庫**：無變更

**測試**：只改文件，未跑回歸

**發現的問題或疑問**：

- 剩餘工作分配表第 0–3、4b、5b、6 項實際進度未逐項查證，請蕾蕾老師或下一位接手者依日誌更新
- `docs/交班文件_給Codex.md` 的「已完成功能」仍停在 9/21（上一筆已提過），這次未改

**下一步**：

- 蕾蕾老師：確認合併

---

## 2026-09-23｜Claude Code｜整合第 4 步進最新 main

**分支**：`claude-step4-integration`（從 main `3a3b174` 開出）
**狀態**：完成，待蕾蕾老師確認合併

**做了什麼**：

- 把 9/21 做完但一直沒合併的分支（`claude/hualien-health-classroom-system-44d1z9`）整合進 9/23 的 main。中間 Codex 做了分析頁、SMART 目標、打卡與教師批改，這次一起收攏。
- 合併前 main 有四個問題，這次一次解掉：
  1. 程式呼叫一支已經不存在的函式。`hc_my_teacher_name()` 在 9/20 依蕾蕾的決定從線上 drop 掉了，但 main 有四處還在呼叫（`api.ts`、`SelfCheck.tsx`、`Analysis.tsx`、`SmartGoal.tsx`）。都有 `catch` 所以頁面沒壞，但每個學生每次開自我檢測／分析／SMART 頁都打一個註定失敗的請求，而且關懷文案退回舊版、多一個「她」。
  2. 匯出 CSV 仍含心理檢測的分數、分級與第 20 題。
  3. 教師端沒有「已聯繫」可以按，L3 的處理紀錄無處可留。
  4. `supabase/migrations/20260920070000_my_teacher_name.sql` 還在資料夾裡，重跑會把已 drop 的函式救回來。
- 帶進來的：紅旗頁三區＋已聯繫＋重複觸發、教師首頁 L3 提示、CSV 只留等級與處理狀態、文案改「老師」不帶姓名、補兩個 migration 檔、刪掉 `my_teacher_name.sql`、把 `docs/協作規則.md` 與 `docs/工作日誌.md` 帶進主線。

**動到的檔案**：

- 合併帶進：`src/health/teacher/FlagList.tsx`（重寫）、`src/health/teacher/L3Alert.tsx`（新增）、`src/health/teacher/csv.ts`、`src/health/api.ts`、`src/health/riskLevel.ts`、`src/health/selfcheck/{riskCopy.ts,RiskCare.tsx,Scale.tsx,SelfCheck.tsx}`、`src/lib/types.ts`、`src/pages/Dashboard.tsx`
- **Codex 的檔案，只動整合必要的三處**：
  - `src/health/analysis/Analysis.tsx`：移除 `myTeacherName` 的 import／state／呼叫，`RiskCare` 不再傳 `teacherName`，範例資料補 `risk_l3_count: 0`
  - `src/health/goal/SmartGoal.tsx`：同上三處
- `supabase/migrations/`：刪 `20260920070000_my_teacher_name.sql`；新增 `20260920070000_risk_l3_count.sql`、`20260921000000_lock_risk_columns.sql`
- `docs/schema.md`（解衝突）、`docs/待辦.md`、`docs/課本檢測_等第對應與紅旗處理規格.md`、`docs/協作規則.md`（新增）、`docs/工作日誌.md`（新增）

**沒動到的檔案**（確認用）：

- `riskLevel.ts` 的判定邏輯（`BANDS`／`riskLevel()`／`riskReasons()`）一個字沒改
- `hc_health_risk_review()`、risk 相關 trigger 未修改
- Codex 的 `engine.ts`、`mentalStrategies.ts`、`BreathingGuide.tsx`、`GratitudeJournal.tsx`、`WeekOneCheckin.tsx`、`GoalReview.tsx`、`Progress.tsx` 完全沒動
- `docs/學生端分析頁_建議庫內容.md`、`suggestions.ts` 未動
- 學生端登記頁、投影頁、`join_code` 免登入流程未動

**資料庫**：**無新增 migration**。補進來的兩個檔案（`risk_l3_count`、`hc_guard_risk`）早在 9/20-21 就套用在正式 Supabase，這次只是讓檔案與資料庫一致，蕾蕾老師不需要再去 SQL Editor 執行任何東西。

**測試**：

- 回歸全過：flag4 52、detail 93、prog 40、preview 29、classlist 16、risklevels 38、riskLevel 單元 46、perf 30、gate 11、csv 12
- 新增 vcopy 9 項（L3 學生端實際畫面的逐字檢查）
- `tsc` 無誤、`npm run build` 通過
- 三項指定驗證見下一節

**發現的問題或疑問**：

1. **prog 有兩項斷言在 main 上就已經失敗**，不是這次合併造成的（我切回 `origin/main` 跑過一次確認）。原因是 Codex 在進度表加了第 10 欄「SMART 目標」，舊斷言寫死「九欄全是 ✓」。已更新測試：種一筆 `confirmed: true` 的目標資料，並把「甲同學不在清單上」改成只看「還沒做完的」那一段（手機版那一區上面多了 SMART 名單，整段文字會含所有人的名字）。
2. **risklevels 有一項斷言太鬆**：原本只比對「老師也會知道你需要關心」前半句，所以舊版多出來的「她」抓不到。已改成整句比對，並加驗不含「她」。
3. **進度表的 SMART 欄位符合投影頁鐵則**：只顯示「✓ 已完成／□ 尚未完成」，沒有分數、沒有目標內容。
4. `docs/交班文件_給Codex.md` 第四節的「已完成功能」與 `docs/待辦.md` 仍停在 9/21 的狀態，這次沒有大改；合併後可以再整理一次。

**下一步**：

- 蕾蕾老師：預覽確認 → 合併 → 部署 → 建一個新的存檔點 tag（`claude-handoff-2026-09-21` 那個在遠端從來沒建起來，我這個環境推不了 tag；替代品是分支 `handoff/claude-2026-09-21`）
- 之後：照片上傳（需先定規格）、教師端四週熱區、期末成果

---

## 2026-09-21｜Claude Code｜第 4 步 教師端 L3 置頂與已聯繫按鈕、risk 欄位鎖定

**分支**：`claude/hualien-health-classroom-system-44d1z9`（三個 commit：`61e3e32`、`ccd85c8`、`8b1ef00`）
**狀態**：完成，待蕾蕾老師確認合併。**尚未合併進 main，也還沒部署到 GitHub Pages。**

**做了什麼**：

- 教師端紅旗頁（`/health/teacher`）重寫成三區：L3 立即置頂 → L2 需關注 → L1 留意（可收合）。未處理的 L3 排最上面，標記「已聯繫」之後排到後面變灰但不消失，那是輔導紀錄。
- 「已聯繫」按鈕：三個處理結果（需要持續關心／課堂觀察後無虞／疑似玩笑）＋一行備註，按錯了有「取消標記」。寫入只走 `hc_health_risk_review()`，碰不到學生的作答。第二次以上觸發 L3 的人標「重複觸發 N 次」。
- 教師端首頁（班級管理頁）最上方加 L3 提示，顯示班級／座號／姓名／觸發時間，只列未處理的，沒有「知道了」之類的關閉鈕。多一個「上課投影時先收起」，收起後只剩「有 N 位」那一行，記在 sessionStorage，關掉分頁就恢復。
- 匯出 CSV 拿掉心理檢測的分數、分級與第 20 題，只留「心理檢測・等級」與「心理檢測・處理狀態」；老師的處理備註也不出。睡眠、85210、飲食金字塔、生活型態照舊。
- 關懷文案的老師稱呼改成「老師」不帶姓名，文案 C 補一句；`riskCopy.ts` 是改規格書之後用程式重新產生的，不是手改。連帶移除 `hc_my_teacher_name()` 這支 RPC，學生端少一條資料庫查詢。
- 補洞：學生原本可以繞過前端直接送 update 把自己的紅旗關掉（RLS 只能限制哪幾列、不能限制哪幾欄）。加了 `hc_guard_risk` trigger。

**動到的檔案**：

- `src/health/teacher/FlagList.tsx`（重寫）
- `src/health/teacher/L3Alert.tsx`（新增）
- `src/health/api.ts`（修改：`listRisk()`／`reviewRisk()` 取代 `listFlagged()`）
- `src/health/teacher/csv.ts`（修改：心理檢測欄位）
- `src/health/riskLevel.ts`（修改，**見下方鎖定清單說明**）
- `src/health/selfcheck/riskCopy.ts`（重新產生，**見下方鎖定清單說明**）
- `src/health/selfcheck/RiskCare.tsx`、`Scale.tsx`、`SelfCheck.tsx`（移除教師姓名那段）
- `src/lib/types.ts`、`src/pages/Dashboard.tsx`
- `supabase/migrations/20260920070000_risk_l3_count.sql`（新增）
- `supabase/migrations/20260921000000_lock_risk_columns.sql`（新增）
- `docs/待辦.md`、`docs/schema.md`、`docs/課本檢測_等第對應與紅旗處理規格.md`

**⚠️ 這次動到的東西有四項在鎖定清單上**（協作規則第五節）。四項都是蕾蕾老師當天明確指示的，列出來給下一個接手者對帳：

| 鎖定項目 | 動了什麼 | 依據 |
|---|---|---|
| `riskLevel.ts` | **判定邏輯一個字沒改**（`BANDS`、`riskLevel()`、`riskReasons()` 原樣）。只在檔尾新增教師端的處理結果常數 `RiskOutcome` / `RISK_OUTCOMES` / `RISK_OUTCOME_LABEL` | 放這裡是因為 `csv.ts` 也要印這些字，而 `csv.ts` 刻意不相依 supabase client |
| `riskCopy.ts` | 重新產生（非手改） | 蕾蕾 9/20：「文案 A、B 裡的老師稱呼改成『老師』，不用名字……請改規格書再重新產生，不要手改」 |
| 含 risk 的 migration／trigger | 新增 `risk_l3_count` 欄位、新增 `hc_guard_risk` trigger | 蕾蕾 9/21：「補洞走甲」 |
| 規格書第五節 | 文案 A／B／C 三處文字、教師端實作註記、不可做的事第 3 點補範圍 | 蕾蕾 9/20 逐句指定的改法 |

**沒動到的檔案**（確認用）：

- `hc_health_risk_review()` 未修改（第 4 步只是呼叫它）
- `src/health/rules.ts`、`src/health/teacher/thresholds.ts`（BMI／體脂／腰圍門檻）未動
- `docs/學生端分析頁_建議庫內容.md` 未動
- `src/health/selfcheck/suggestions.ts` 未動
- 學生端登記頁、班級進度表（投影頁）、點名與座位的 join_code 免登入流程未動
- 進度表仍然完全唯讀，仍然不顯示任何心理紅旗

**資料庫**：新增兩個 migration，**都已套用到線上 Supabase**（所以線上資料庫目前比 main 的程式超前；舊程式不讀新欄位，不會壞）。

還原 SQL：

```sql
-- 還原 20260921000000_lock_risk_columns.sql
drop trigger if exists hc_guard_risk on public.hc_health_selfcheck;
drop function if exists public.hc_guard_risk_columns();

-- 還原 20260920070000_risk_l3_count.sql
alter table public.hc_health_selfcheck drop column if exists risk_l3_count;
```

**測試**：

- 新增 `flag4` 52 項，全過（三區順序、未處理置頂、重複觸發、RPC 參數、只走 RPC 不動資料表、RPC 失敗不假裝成功、首頁提示、收起、沒有 L3 時不顯示、學生被擋、空狀態）
- 線上資料庫 11 項，全過（兩個 transaction 做完 rollback，含對照組）：學生改不動處理紀錄四欄、調不低 `risk_level`、黏旗與觸發時間清不掉、新增時被強制成未處理；對照組：作答欄位仍然改得動、等級往上寫得進去、改不到別人的列、老師的 RPC 標記與取消都仍有效
- 回歸全過：detail 93、prog 40、preview 29、classlist 16、risklevels 38、riskLevel 單元 46、perf 30、gate 11、csv 12
- `tsc` 與 `build` 都過
- 過程中修了三支測試自己的問題（不是程式的問題）：`risklevels` 還在斷言文案裡有老師姓名、`risk.ts` 的原始碼掃描把註解裡的字當成程式碼、`csv.test.mjs` 產生的暫存檔跳脫字元壞掉跑不起來

**發現的問題或疑問**：

1. **協作規則第二節說的存檔點 `claude-handoff-2026-09-21` 這個 tag 在遠端不存在。** 我這個執行環境的代理擋住 tag 推送（HTTP 403），連 GitHub 建 ref 的 API 也擋。替代品是分支 **`handoff/claude-2026-09-21` → `8e2707e`**，內容相同，回復方式一樣。要真正的 tag 請蕾蕾老師自己在 GitHub 網頁上建（Releases → 新 release，tag 名 `claude-handoff-2026-09-21`，target 填 `8e2707e`）。**在那之前，協作規則第二節提到的名字是指不到東西的。**
2. **那個存檔點不含第 4 步。** `8e2707e` 是第 1-3 步 ＋ 交班文件。合併第 4 步之後應該再打一個新的存檔點。
3. **第 0 項（72 筆重進結果頁）我已經回答過了**：會看到，結果頁每次都從資料庫重建（`getSelfcheck` → `outcomeFromRow`），而且 `risk` 是用那一列的分數**現算**的，不是讀 `risk_level` 欄位；兩者在那 71 筆對得上名單的資料上完全一致。前提是這個分支要先合併部署。Codex 若重做這一項，可以當交叉檢查——答案不一樣反而是有用的訊號。
4. **學生端與教師端的等級來源不同**：學生端現算、教師端讀 `risk_level`。加了 monotonic trigger 之後，重做而分數下降的人會出現「老師看到 L2、學生自己看到 L1 文案」。目前沒有這種資料。教師端卡片已經加一行灰字說明，學生端維持現算（蕾蕾未裁示改動）。
5. **今天有一次直接推 main**：交班文件 `8e2707e`，是蕾蕾老師明確指示的，發生在這份協作規則生效之前。之後遵守「不可直接改 main」。

**下一步**：

- **蕾蕾老師**：合併 `claude/hualien-health-classroom-system-44d1z9` → 部署；建 `claude-handoff-2026-09-21` tag；決定這兩份協作文件要不要合併進 main（在合併之前，Codex 從 main 是讀不到 `docs/協作規則.md` 的）
- **Codex**：第 0 項（交叉檢查上面第 3 點）、第 1 項 生活型態 10 題接建議庫
- **Claude Code**：第 4 項 `hc_health_goal` 表 + RLS（等蕾蕾老師說）
