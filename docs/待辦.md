# 待辦

## 已完成：教師端紅旗名單與 L3 置頂（2026-09-20）

原本寫在這裡的「最優先：紅旗沒有人在看」已經解決，做法照
`docs/課本檢測_等第對應與紅旗處理規格.md` 第五節：

- `src/health/teacher/FlagList.tsx` —— 三區（L3 立即／L2 需關注／L1 留意，L1 可收合），
  未處理的 L3 排最上面，標記已聯繫之後仍留在名單裡不會消失
- `src/health/teacher/L3Alert.tsx` —— 教師端首頁最上方的 L3 提示，
  只列未處理的，顯示班級／座號／姓名／觸發時間
- 「已聯繫」寫入走 `hc_health_risk_review()`，處理結果三選一
  （需要持續關心／課堂觀察後無虞／疑似玩笑）＋一行備註
- `risk_l3_count >= 2` 標「重複觸發 N 次」

當初這一節強調的「分數高與 depression_critical 不能混成同一種紅點」已經內建在
等級裡：第 20 題答是一律 L3，分數高最多到 L2，兩者在畫面上是不同顏色不同區塊。
`depression_critical` 仍然是黏的（一旦為 true 就不會被重測覆蓋回 false），
「已聯繫」關掉的是**處理狀態**，不是旗子本身。

## 中優先【需蕾蕾決定】：學生改得動自己那一列的 risk_* 欄位

`hc_health_selfcheck` 上學生的 policy 是

```sql
hc_health_selfcheck_student_own  ALL  USING ((auth.jwt()->>'email') = student_email)
```

**ALL**，而且 RLS 只能限制「哪幾列」、不能限制「哪幾欄」。所以一個會開
開發者工具的學生，可以對自己那一列直接送一個 update，把
`risk_level` 改成 0、或把 `risk_reviewed` 改成 true——旗子就從教師端消失了。
（老師那邊只有 SELECT policy，改不到別人的資料；這一條說的是學生改自己的。）

實際風險不高（要會用 PostgREST、要知道欄位名），但被藏掉的正好是
第 20 題那一級，跟「誤放的代價比誤抓高太多」直接衝突，所以記在這裡。

幾種解法，動到的範圍不一樣：

| 解法 | 做什麼 | 動到 |
|---|---|---|
| 甲 | BEFORE UPDATE trigger 鎖住四個**處理紀錄**欄位（`risk_reviewed`／`risk_reviewed_at`／`risk_outcome`／`risk_note`），只有 `hc_health_risk_review()` 改得動 | 一個 migration、一支 trigger function；學生端與教師端程式都不用改 |
| 乙 | 連 `risk_level`／`risk_flagged_at`／`risk_l3_count` 也改成資料庫端現算（trigger 依 mood／stress／depression 三欄重算），學生端送什麼都會被蓋掉 | 判定規則要在 SQL 再寫一份，跟 `riskLevel.ts` 變成兩份要同步——改門檻時得改兩邊 |
| 丙 | 甲＋乙 | 同上 |
| 丁 | 先不動，記著 | — |

建議甲：擋住最要緊的那一半，而且不會把判定邏輯複製成兩份。
乙的那三個欄位要不要一起鎖，等真的有人動手腳再說——
真要防，`riskLevel.ts` 的規則搬進資料庫才是根治，那是另一件事。

## 中優先：hc_classes 的 WITH CHECK 沒有檢查 hc_teachers

```sql
-- 目前的 policy
hc_classes_owner  ALL  WITH CHECK (teacher_id = auth.uid() OR hc_is_admin())
```

這條**完全沒有查 `hc_teachers`**。任何登入成功的帳號（含學生）只要能叫到
PostgREST，就可以插入一列 `teacher_id = auth.uid()` 的班級；插進去之後
`hc_owns_class()` 對那個班就成立，接著可以加學生、開課堂、記出缺席。

2026-09-12 刪掉三列誤建的 `hc_teachers`（s310055／s310189／s310247，
三位都是學生，0 個班級）之後，他們的畫面不再是教師後台了。
但那是**關門，不是上鎖**——門閂還在門外。

### 為什麼不急

- RLS 仍然把讀取範圍框死在「自己建的」。實測過：用那三個帳號的身分查詢，
  班級／學生／自我檢測全是 0，`hc_teaches_student_email(別人)` 回 false。
  **讀不到任何別人的資料，不會外洩。**
- 最壞的情況是資料表長出沒人要的垃圾班級。
- 而且現在學生登入後會被導到 `/health`，看不到建班的介面，要刻意繞才做得到。

### 要動的時候要注意

改 WITH CHECK 加上「必須是 hc_teachers 裡的人」是一個 migration，
會動到**所有人的建班路徑**。動之前要先確認沒有別的流程依賴現在的寬鬆條件
（例如老師第一次登入時 `hc_ensure_teacher()` 與第一次建班的先後順序）。
還沒查過，不要憑印象改。

## 其他

- 多元選修的 GAS 同步：需要原班欄位，改用「班級＋座號」比對列
- BMI／腰臀比目前只有 17.5–18 歲男生的門檻（`src/health/rules.ts`），
  要支援女生與其他年齡，得先有性別與年齡資料（`hc_students.gender` 目前全空）

## 已解決：BMI 門檻換成官方表格（2026-09-20）

舊的 17.7 / 21.9 / 23.2 對不上任何一列，已全部換掉。
現在用的是蕾蕾從課本查到的官方表格：
**衛生福利部國民健康署，2021，育達版健康與護理課本 P.24（男性）**

| 年齡 | 過輕 < | 過重 ≥ | 肥胖 ≥ |
|---|---|---|---|
| 15 | 16.9 | 22.9 | 25.4 |
| 16 | 17.4 | 23.3 | 25.6 |
| 17 | 17.8 | 23.5 | 25.6 |
| 18 | 18.5 | 24.0 | 27.0 |

年齡取法寫成表（`GRADE_ROUND_AGE`）：高三期初查 17 歲、期末查 18 歲；
期中與期初同列（那時只有一部分人過完生日，取較嚴的）。換年級改那張表就好。

年級不可信時（`hc_classes.grade_confirmed = false`）一律不判 BMI，
畫面顯示「年級未確認」。套錯一列的代價是實的：15 歲過重 22.9、18 歲 24.0。

**教訓：門檻數字沒有一手出處就不要寫進程式。** 舊那組是憑印象寫的，
commit 訊息裡沒有任何查證紀錄，撐了兩週才被本校資料的分布抓出來。

### 學生端年級：白名單放進非高三班級之前要先處理

`hc_my_student_profile()` 沒有回傳 grade，而學生讀不到 `hc_classes`
（RLS 只開給帶班的老師），所以學生端目前用 `rules.ts` 的 `STUDENT_GRADE = 3`。

**現在不急，因為健康模組白名單（`hc_classes.health_enabled`）裡只有
305–309 五個高三班。** 學生端會看到的年齡列因此一定是對的。

⚠️ **未來如果把其他年級的班級加進白名單，一定要先改
`hc_my_student_profile()` 讓它回傳年級**，否則高一高二的學生會被套上
高三（17／18 歲）的 BMI 門檻。15 歲那列過重是 22.9、18 歲是 24.0，
差 1.1，會把正常的判成過重。

改法：那支函式已經 join 了 `hc_classes`，多回一欄 `c.grade`
（順便可以回 `c.grade_confirmed`）。注意那是 DROP + CREATE——
PostgreSQL 不能用 CREATE OR REPLACE 改 RETURNS TABLE 的欄位——
改完要補回原本的
`revoke execute ... from public, anon` 與 `grant execute ... to authenticated`。

### 健康模組白名單（2026-09-20）

`hc_classes.health_enabled`，預設 **false**（新班要自己開）。
從班級管理的編輯畫面勾選，沒有寫死在程式裡。

目前開啟：305、306、307、308、309。
關閉：基礎急救概論（多元選修，上的是急救，之後有自己的模組）、測試班。

擋兩層：
1. 前端 —— `hc_health_enabled_for_me()`，沒開的班學生看到說明卡而不是表單
2. 資料庫 —— `hc_is_known_student_email()` 加上 `health_enabled` 條件，
   兩張健康表的 WITH CHECK 都走它，所以知道網址也寫不進去

⚠️ **測試班也被擋住了。** 要再用老師帳號實測學生流程的話，
先到班級管理把測試班的「使用健康管理模組」勾起來，測完再取消。
不想改資料的話，教師後台的「以學生身分預覽」不受影響（那條路不寫資料庫）。

## 已定案：教師端的紅／琥珀分層原則

**紅色＝今天要處理**（血壓、血氧、脈搏：請學生休息後重測，必要時當天帶去保健室）。
**琥珀＝長期指標，課堂上談**（BMI、體脂率、腰圍、腰臀比）。
加新欄位時照這個問題判斷：「看到這個數字，老師今天做得了什麼？」
做得了 → 紅，做不了 → 琥珀。實作在 `src/health/teacher/thresholds.ts`。

體脂率與腰圍的門檻（2026-09-20 由蕾蕾提供，當天又修正過兩處）：
體脂率只標偏高那一側（> 20% 略高、≥ 25% 偏高），課本的「14–20% 正常」
那一段的**偏低側不標**——那是 30 歲以下成人的範圍，用在發育中的高中男生
會大量誤判，而且偏低不是這群人要提醒的方向。
腰圍用 **≥ 90**（國健署代謝症候群的判定門檻），剛好卡 90.0 的那 7 個人也標起來。

（下面是當初提出這件事時的實測數字，留著當背景。）
用 2026-09-19 的 158 筆期初資料實算：

| 項目 | 紅（≥23.2） | 琥珀（21.9–23.2） | 偏低（<17.7） |
|---|---|---|---|
| BMI | 60 人 | 18 人 | 16 人 |

整列被標紅的比例：305 有 12/27、306 有 14/32、307 有 14/27、308 有 17/36、309 有 15/35，
**大約每兩人就有一人**。其中絕大多數是 BMI 造成的——把 BMI 拿掉只剩 2～7 人／班。
原型用的成人門檻 27 則是 1～7 人／班。

選的是方向二（教師端 BMI 只標琥珀）。理由不是紅標太多，是紅色的定義應該是
「今天要處理」，BMI 是長期指標，不需要當天行動。

## 第二、三批規格的後續（`docs/健康管理系統_第二三批規格.md`）

- 第 2 步：建議庫 `suggestions.ts` + 學生端分析頁（純前端）
- 第 3 步：教師端百分位與班級分布圖（依賴明細頁，已完成）
- 第 4～6 步：`hc_health_goal` / `hc_health_checkin` 與目標精靈、打卡頁、打卡看板
